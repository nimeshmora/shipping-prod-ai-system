
/* ===========================================================================
   v2 additions — the story style
   ======================================================================== */

/* a chapter opener: big prose, no diagram, sets the scene */
.chap{max-width:1000px;}
.chap .num{font-family:var(--mono);font-size:12px;letter-spacing:.18em;
  text-transform:uppercase;color:var(--brand);margin-bottom:18px;}
.chap h1{font-size:60px;line-height:1.04;letter-spacing:-.035em;margin:0 0 22px;
  font-weight:700;text-wrap:balance;}
.chap .lede{font-size:26px;line-height:1.42;color:var(--ink-dim);max-width:830px;}
.chap .lede b{color:#fff;}
.chap .clock{margin-top:26px;font-family:var(--mono);font-size:13px;
  color:var(--ink-faint);letter-spacing:.06em;}

/* narrative slide: one paragraph, told not listed */
.tale{font-size:30px;line-height:1.4;letter-spacing:-.015em;max-width:940px;}
.tale b{color:#fff;}
.tale .q{color:var(--brand);}

/* THE SPINE — the recurring picture that grows one layer per chapter */
.spine{display:flex;flex-direction:column;align-items:center;gap:0;width:100%;}
.spine .ring{border:2px solid var(--rule);border-radius:14px;width:100%;
  padding:13px 15px 15px;background:#131316;position:relative;}
.spine .ring + .ring{margin-top:0;}
.spine .rt{font-family:var(--mono);font-size:11px;letter-spacing:.15em;
  text-transform:uppercase;color:var(--ink-faint);margin-bottom:7px;}
.spine .rw{font-size:17px;line-height:1.3;color:var(--ink-faint);}
/* a layer that already existed */
.spine .ring.was{border-color:var(--rule);background:#15151a;}
.spine .ring.was > .rt{color:var(--ink-faint);}
.spine .ring.was > .rw{color:var(--ink-faint);}
/* the layer this chapter just added */
.spine .ring.new2{border-color:var(--brand);background:rgba(244,102,34,.15);
  box-shadow:0 0 0 1px rgba(244,102,34,.35);}
.spine .ring.new2 > .rt{color:var(--brand);}
.spine .ring.new2 > .rw{color:var(--ink);}
.spine .ring.new2 > .rw b{color:#fff;}
/* the agent at the centre, always */
.spine .ring.core3{border-color:var(--ok);background:rgba(95,191,127,.13);}
.spine .ring.core3 > .rt{color:var(--ok);}
.spine .ring.core3 > .rw{color:var(--ink);}
.spine .ring.core3 > .rw b{color:#fff;}
/* still to come */
.spine .ring.soon{border-style:dashed;border-color:#2a2a32;background:transparent;}
.spine .ring.soon > .rt{color:#4a4850;}
.spine .ring.soon > .rw{color:#4a4850;}

/* the horizontal version, for the four-hour overview */
.grow{display:flex;align-items:stretch;gap:0;width:100%;}
.grow .st{flex:1;border:2px solid var(--rule);border-radius:12px;padding:14px 12px;
  background:#131316;text-align:center;display:flex;flex-direction:column;
  justify-content:flex-start;gap:7px;}
.grow .st + .st{margin-left:-2px;}
.grow .st .h{font-family:var(--mono);font-size:10.5px;letter-spacing:.13em;
  text-transform:uppercase;color:var(--ink-faint);}
.grow .st .pic{font-size:13px;line-height:1.5;color:var(--ink-dim);
  font-family:var(--mono);}
.grow .st .w{font-size:14.5px;line-height:1.32;color:var(--ink-dim);}
.grow .st.on4{border-color:var(--brand);background:rgba(244,102,34,.14);z-index:2;}
.grow .st.on4 .h{color:var(--brand);}
.grow .st.on4 .w{color:var(--ink);}
.grow .st.on4 .w b{color:#fff;}
.grow .st.done4 .h{color:var(--ok);}

/* a before/after pair, drawn plainly */
.thenow{display:grid;grid-template-columns:1fr auto 1fr;gap:20px;align-items:center;
  width:100%;}
.thenow .col{border:2px solid var(--rule);border-radius:14px;padding:20px;
  background:#131316;}
.thenow .col.b2{border-color:var(--ok);background:rgba(95,191,127,.09);}
.thenow .col .lb{font-family:var(--mono);font-size:11px;letter-spacing:.14em;
  text-transform:uppercase;color:var(--ink-faint);margin-bottom:12px;}
.thenow .col.b2 .lb{color:var(--ok);}
.thenow .col .big2{font-size:23px;line-height:1.34;color:var(--ink-dim);}
.thenow .col .big2 b{color:#fff;}
.thenow .mid3{font-family:var(--mono);font-size:26px;color:var(--brand);}
